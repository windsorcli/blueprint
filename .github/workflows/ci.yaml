name: CI

on:
  push:
    branches:
      - main
      - workflows
  pull_request:
    branches:
      - main

env:
  windsorblueprint_version: latest

jobs:
  # Blueprint Checks
  blueprint-checks:
      uses: windsorcli/blueprint/.github/workflows/blueprint-checks.yaml@main

  blueprint-checkov:
      uses: windsorcli/blueprint/.github/workflows/blueprint-checkov.yaml@main
        
